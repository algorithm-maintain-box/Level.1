#include <string>
#include <vector>

using namespace std;

vector<string> solution(vector<string> files)
{
    for (auto iter : files)
    {
        for (auto i = 0; i < iter.size(); i++)
        {
            if (iter[i] < 'a')
            {

            }
        }
    }

    vector<string> answer;
    return answer;
}